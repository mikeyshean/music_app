<h1>Log in!</h1>
<%= flash_notice %>
<form action="<%= session_url %>" method="post">
  <%= authenticity_token %>
  <%= render 'users/form', user: @user %>
  New user? <a href="<%= new_user_url %>">Sign up!</a>
